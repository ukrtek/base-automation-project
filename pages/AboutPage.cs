using OpenQA.Selenium.Remote;

namespace base_automation_project.Pages
{
    public class AboutPage : ParentPage
    {
        public AboutPage(RemoteWebDriver driver) : base(driver)
        {
        }
    }
}